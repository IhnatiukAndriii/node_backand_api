import { Request, Response } from 'express';

// Placeholder for future habits CRUD controllers
export async function getHabits(req: Request, res: Response) {
  return res.status(200).json({ habits: [] });
}
